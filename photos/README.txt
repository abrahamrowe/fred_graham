Put your photo files (.jpg, .png) in this folder.

Then, for each photo, add a line in  data/photos.js  so it shows
up in the gallery. The example entries in that file show the pattern.

Tip: keep filenames simple — no spaces. Use dashes instead, e.g.
  fred-at-the-lake.jpg
